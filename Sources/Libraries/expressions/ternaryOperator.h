   #pragma once

#include "expression.h"

namespace Expressions{

	//class TernaryOperator: public Expression
	//{
	//public:
	//	TernaryOperator(const Expression* condition, const Expression* first, const Expression* second);

	//	virtual const Expression* evaluated(const NameScope& environment, boost::any* userData = 0) const override;

	//private:
	//	const Expression* condition;
	//	const Expression* first;
	//	const Expression* second;
	//};

}//
