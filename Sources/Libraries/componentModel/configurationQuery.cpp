#include "configurationQuery.h"


//
//
//
/*
std::ostream& operator<<(std::ostream& os, const FinalizeQuery::Entry& entry)
{
	os << "\tentry:\n" << "\t\tclassId: " << entry.clsId << "\n";
	if(!entry.alias.empty()) os << "\t\talias: " << entry.alias << "\n";
	if(entry.index != 0) os << "\t\tindex: " << entry.index << "\n";	

	return os;
}

std::ostream& operator<<(std::ostream& os, const FinalizeQuery& query)
{
	query.dump(os);
	return os;
}*/
