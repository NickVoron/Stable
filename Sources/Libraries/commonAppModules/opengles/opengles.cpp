#pragma once

#include "imguiModules/library.include.h"
#include "opengles.h"

#include "../winInput.h"

namespace AppModules
{
	/*
	//
	//
	//

	OpenGLESFrameBegin::Params::Params()
	{
		color	= Vector4(0.7f, 0.425f, 0.0f, 1.0f);
		depth	= 1.0f;
		stencil	= 0;

		clearDepth		= true;
		clearColor		= true;
		clearStencil	= false;
	}

	//
	//
	//
	void OpenGLES::create(Window::Params& windowParams, Params& params)
	{
		windowParams.createWindows(params.windows);
	}

	void OpenGLES::release()
	{		
	}


	//
	//
	//
	void OpenGLESFrameBegin::create(Params& params)
	{

	}

	void OpenGLESFrameBegin::update(Params& params, WindowsGAPI::WindowsGLES& windows)
	{
 		windows.clear();
	}

	//
	//
	//
	void OpenGLESFrameEnd::process(WindowsGAPI::WindowsGLES& windows)
	{
		windows.present();
	}

	*/
}
