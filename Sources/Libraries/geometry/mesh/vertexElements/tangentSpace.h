#pragma once

#include <math.h>
#include "vector.h"

namespace Geometry
{
	BASIS_COMPONENT_TYPE(NORMAL,	NORMAL, float, 5.0f);
	BASIS_COMPONENT_TYPE(BINORMAL,	BINORMAL, float, 5.0f);
	BASIS_COMPONENT_TYPE(TANGENT,	TANGENT, float, 5.0f);
}